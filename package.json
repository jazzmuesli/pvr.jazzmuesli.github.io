{
  "name": "pv-calc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PV + battery export revenue simulator (Marktprämie / Direktvermarktung)",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}
